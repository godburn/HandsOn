
public static class EnMach {
    
    public enum Look {
        none,
        rollover,
        info
    }
    
    /*
    public enum Status {
        off,
        on,
        run,
        runBad
    }
    public enum Mouse {
        none,
        machine,
        panel,
        load,
        lid,
        cover
    }
    */
    public enum SpinMode {
        off,
        up,
        hold,
        down
    }

    public enum EditMode {
        none, rpm, time
    }

    public enum Part {
        none,
        tooltip,
        machine,
        info,
        lid
    }
    
    public enum Butt {
        none
    }

}

